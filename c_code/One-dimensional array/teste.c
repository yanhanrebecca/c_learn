#include <stdio.h>
int main()
{
    int i, n, search_success,k,a[100],tt, option_num,t=0;    
    //按键盘输入初始化一个含有n个整数的一维数组a[]
    printf("请输入数组元素个数：\n");
    scanf("%d", &n);
    printf("请输入%d个数：\n", n);
    for(i = 0; i < n; i++){
        scanf("%d", &a[i]);
    } 
    }      